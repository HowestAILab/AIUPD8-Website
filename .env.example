# Sanity configuration
SANITY_PROJECT_ID="" 				# Provide a value for SANITY_PROJECT_ID
SANITY_DATASET="" 				# Provide a value for SANITY_DATASET
SANITY_TOKEN="" 				# Provide a value for SANITY_TOKEN

# API Base URL – leave empty. The Nuxt server routes (/api/tools, /api/blog …)
# are served from the same host in both dev and production, so no explicit base URL
# is needed. Only set this if you are proxying to a separate API server.
API_BASE_URL=""

# OpenAI – SERVER-SIDE ONLY. Never expose this to the browser.
# Used by /server/api/translate.post.ts to power the auto-translate button in Sanity Studio.
OPENAI_API_KEY=""
